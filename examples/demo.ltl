// examples/demo.ltl — runnable demo for ltl_kakuro

import std::io
use ltl_kakuro as lib

fn main() {
    io::println("ltl_kakuro demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}